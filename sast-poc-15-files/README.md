# Intentionally Vulnerable SAST POC Corpus

15 deliberately vulnerable, non-production source files for controlled SAST demos. Contains 25+ repeated finding instances/categories such as command injection, SQL injection, SSRF, traversal, hard-coded secrets, weak hashes/crypto, insecure randomness, unsafe deserialization, eval/dynamic execution, XSS-style output, and sensitive logging.
